import '../../core.dart';

/// Centralized color palette for the application.
/// 
/// Defines all custom colors used in the design system to ensure visual consistency.
class AppColorPalette {
  /// Primary brand color.
  static const Color primary = Color(0xFF035C4D);
  
  /// Solid black color.
  static const Color black = Colors.black;

  /// Solid white color.
  static const Color white = Colors.white;
  
  /// Background color for dark theme components.
  static const Color darkSurface = Color(0xFF1C1B1F);
  
  /// Background color for light theme components.
  static const Color lightSurface = Color(0xFFF4F8F7);
  
  /// Standard grey color.
  static const Color grey = Color(0xFF808080);

  /// Light grey color for borders and disabled states.
  static const Color lightGrey = Color(0xFFCDCDCD);

  /// Dark grey color for text or subtle surfaces.
  static const Color darkGrey = Color(0xFF3A3A3A);
  
  /// Color used for error states and destructive actions.
  static const Color red = Color(0xFFDD5050);
  
  /// Color used for success states.
  static const Color green = Color(0xFF27AA1A);
  
  /// Blue accent color for highlighting.
  static const Color blueAccent = Colors.blueAccent;

  /// Orange accent color for secondary actions.
  static const Color orangeAccent = Colors.orangeAccent;

  /// Transparent color.
  static const Color transparent = Colors.transparent;

  /// Standard blue color.
  static const Color blue = Color(0xFF2381C4);
}
