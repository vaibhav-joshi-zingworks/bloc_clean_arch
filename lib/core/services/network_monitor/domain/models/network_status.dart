/// Represents the binary state of device internet connectivity.
enum NetworkStatus { 
  /// The device has a valid, functional connection to the internet.
  connected, 
  
  /// The device is offline or the connection is non-functional.
  disconnected 
}
